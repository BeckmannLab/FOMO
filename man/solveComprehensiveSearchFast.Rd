% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-solve.R
\name{solveComprehensiveSearchFast}
\alias{solveComprehensiveSearchFast}
\title{Comprehensive Search Sample Relabeling (Fast)}
\usage{
solveComprehensiveSearchFast(object, max_genotypes = 8)
}
\arguments{
\item{object}{A MislabelSolver object}

\item{max_genotypes}{(Default = 8) The number of combinations scales in factorial
with the number of genotypes in the largest connected component.
The algorithm will skip over all components that exceed this size.}
}
\value{
A MislabelSolver object
}
\description{
A drop-in, numerically exact replacement for \code{\link[=solveComprehensiveSearch]{solveComprehensiveSearch()}}
that scores permutations without \code{\link[reshape2:melt]{reshape2::melt()}}/\code{dplyr} joins on the
full genotype-by-permutation table. Two independent changes: (1) a
component's \emph{locked} genotype columns (already resolved before
comprehensive search reached this component) are constant across every
permutation, so their score contribution is computed once from a single
row instead of melted and joined on every one of up to 8! = 40,320
permutation rows; (2) the per-swap-category scoring itself is done with
named-vector lookups and \code{rowsum()} instead of
\code{dplyr::left_join()}/\code{group_by()}/\code{summarize()}, since profiling showed
the original's cost dominated by join/data-mask overhead rather than the
arithmetic. Verified against \code{\link[=solveComprehensiveSearch]{solveComprehensiveSearch()}} on real
mid-solve state (8 free / 16 locked genotypes, 40,320 permutations, 6
swap categories): identical scores and identical best permutation, ~16x
faster on that case.
}
\details{
Use via \code{solveEnsemble(object, use_solvers = c("majority", "comprehensive_fast", "local"))} rather than calling this directly,
unless you are composing your own solver loop.
}
