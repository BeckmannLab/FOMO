% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate-n-mislabels.R
\name{estimate_n_mislabels}
\alias{estimate_n_mislabels}
\title{Estimate the total number of mislabels based on known sex mismatches}
\usage{
estimate_n_mislabels(
  reported_sex,
  inferred_sex,
  return_fraction = FALSE,
  adjust_for_unevaluable = TRUE
)
}
\arguments{
\item{reported_sex}{Vector of reported sex for each sample.}

\item{inferred_sex}{Vector of inferred sex for each sample. Must be the same
length and same order as \code{reported_sex}.}

\item{return_fraction}{If TRUE, return the estimated fraction of mislabeled
samples instead of the estimated number.}

\item{adjust_for_unevaluable}{If TRUE (the default), then the estimated
number of mislabels will include mislabels of samples with missing sex
information (i.e., NA values in \code{reported_sex} or \code{inferred_sex}. If FALSE,
then the estimate will only include mislabels of samples with fully known
sex information. Note: this argument has no effect if \code{return_fraction} is
TRUE.}
}
\value{
The estimated number (or fraction) of mislabeled samples.
}
\description{
Estimate the total number of mislabels based on known sex mismatches
}
\details{
If \code{reported_sex} or \code{inferred_sex} contains NAs, those samples will
not be used to estimate the fraction of mislabeled samples
}
\examples{

# Simulate 100 samples with 20 mislabels
set.seed(1986)
reported <- sample(c("m", "f"), 100, replace = TRUE)
# Simulate 50 more samples with unknown sex
reported <- c(reported, rep(NA, 50))
inferred <- reported
# Simulate mislabels
inferred[1:20] <- sample(inferred[1:20])
# Should give approximately 30
estimate_n_mislabels(reported, inferred)
# Should give approximately 20
estimate_n_mislabels(reported, inferred, adjust_for_unevaluable = FALSE)
# Should give approximately 20\% (0.2)
estimate_n_mislabels(reported, inferred, return_fraction = TRUE)
}
